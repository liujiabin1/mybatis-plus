# 项目环境配置
profile-env: ${profile-env}

# apollo
app:
  id: ${project.name}

apollo:
  env: fat
  meta: http://apollo-configservice-fat.riverstar.cn:18080
  bootstrap:
    enabled: true
    namespaces: application.yml